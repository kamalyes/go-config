server:
  # 模块名称
  modulename: "qm6kO1gMzD"
  # 端点
  endpoint: "C2q1VmvFcp"
  # 主机地址
  host: "rS7nXB62q0"
  # 端口号
  port: 61
  # 读取超时
  read-timeout: 19
  # 写入超时
  write-timeout: 41
  # 空闲超时
  idle-timeout: 62
  # gRPC 端口
  grpc-port: 24
  # 是否启用 HTTP
  enable-http: true
  # 是否启用 gRPC
  enable-grpc: true
  # 是否启用 TLS
  enable-tls: true
  tls:
    # 证书文件
    cert-file: "pZ8OJBA6sz"
    # 密钥文件
    key-file: "2y28KmDlql"
    # CA 文件
    ca-file: "mawTCmXLJj"

server:
  # 模块名称
  modulename: "common"
  # 服务绑定的IP
  host: "0.0.0.0"
  # 端口
  port: 8880
  # 服务名称
  service-name: xxxx-server
  # 服务根路径
  context-path: /xxx
  #是否开启请求方式检测
  handle-method-not-allowed: true
  #数据库类型
  data-driver: 'mysql'
# cors模块配置文件
# 模块名称: cors
# 包名: cors  
# 描述: CORS跨域模块
# 生成时间: 2025-11-23 23:53:28
# 注意: 此文件由配置生成器自动生成

# allowed origins配置
allowed_origins:
    - '*'
# allowed methods配置
allowed_methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
# allowed headers配置
allowed_headers:
    - Origin
    - Content-Length
    - Content-Type
    - Authorization
# max age配置
max_age: "86400"
# allowed all origins配置
allowed_all_origins: false
# allowed all methods配置
allowed_all_methods: false
# exposed headers配置
exposed_headers: []
# allow credentials配置
allow_credentials: true
# options response code配置
options_response_code: 200
# module name配置
modulename: cors

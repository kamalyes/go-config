# jobs模块配置文件
# 模块名称: jobs
# 包名: jobs  
# 描述: 任务调度模块
# 生成时间: 2025-12-11 13:36:01
# 注意: 此文件由配置生成器自动生成

# 是否启用Job管理器
enabled: true
# 时区配置(例如: Asia/Shanghai)
timezone: Asia/Shanghai
# 优雅关闭超时时间(秒)
graceful-shutdown: 30
# 失败最大重试次数
max-retries: 3
# 重试间隔(秒)
retry-interval: 5
# 重试间隔抖动百分比(0-1)
retry-jitter: 0
# 最大并发任务数，0表示不限制
max-concurrent-jobs: 0
# 是否启用分布式调度
enable-distribute: false
# 任务配置
tasks:
    cleanup:
        # 是否启用Job管理器
        enabled: true
        cron-spec: 0 */5 * * * *
        immediate-start: false
        timeout: 300
        overlap-prevent: true
        # 失败最大重试次数
        max-retries: 3
        # 重试间隔(秒)
        retry-interval: 10
        # 重试间隔抖动百分比(0-1)
        retry-jitter: 0
        priority: 0
        dependencies: []
        max-concurrent: 0
        tags: []
        description: 定期清理过期数据和无效缓存
        breaker:
            # 是否启用Job管理器
            enabled: true
            max-failures: 5
            reset-timeout: 30
            half-open-successes: 2
    data-sync:
        # 是否启用Job管理器
        enabled: true
        cron-spec: 0 0 * * * *
        immediate-start: false
        timeout: 600
        overlap-prevent: true
        # 失败最大重试次数
        max-retries: 3
        # 重试间隔(秒)
        retry-interval: 15
        # 重试间隔抖动百分比(0-1)
        retry-jitter: 0
        priority: 0
        dependencies:
            - task-name: cleanup
              inline: null
            - task-name: ""
              inline:
                # 是否启用Job管理器
                enabled: true
                cron-spec: 0 */30 * * * *
                immediate-start: false
                timeout: 60
                overlap-prevent: true
                # 失败最大重试次数
                max-retries: 0
                # 重试间隔(秒)
                retry-interval: 0
                # 重试间隔抖动百分比(0-1)
                retry-jitter: 0
                priority: 0
                dependencies: []
                max-concurrent: 0
                tags: []
                description: 数据验证前置任务
                breaker:
                    # 是否启用Job管理器
                    enabled: false
                    max-failures: 0
                    reset-timeout: 0
                    half-open-successes: 0
        max-concurrent: 0
        tags: []
        description: 同步数据到远程服务器
        breaker:
            # 是否启用Job管理器
            enabled: true
            max-failures: 3
            reset-timeout: 60
            half-open-successes: 2

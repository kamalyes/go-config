server:
  # 模块名称
  modulename: "common"
  # 服务绑定的IP&端口
  endpoint: "0.0.0.0:8880"
  # 服务名称
  server-name: xxxx-server
  # 服务根路径
  context-path: /xxx
  #是否开启请求方式检测
  handle-method-not-allowed: true
  #数据库类型
  data-driver: 'mysql'
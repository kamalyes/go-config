# sts模块配置文件
# 模块名称: sts
# 包名: sts  
# 描述: STS临时凭证
# 生成时间: 2025-11-28 00:26:09
# 注意: 此文件由配置生成器自动生成

# 模块名称
module-name: aliyun-sts
# 区域 ID
region-id: cn-hangzhou
# 访问密钥 ID
access-key-id: demo_access_key_id
# 访问密钥 Secret
access-key-secret: demo_access_key_secret
# 角色 ARN
role-arn: acs:ram::account:role/demo-role
# 角色会话名称
role-session-name: default-session
